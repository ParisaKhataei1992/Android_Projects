<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.assignment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ItemFragment"></activity>
        <activity android:name=".ItemDetailsFragment" />
        <activity android:name=".Dynamic" />
        <activity android:name=".DeleteItem" />
        <activity android:name=".AddItem" />
        <activity
            android:name=".Assignment2"
            android:label="@string/title_activity_assignment2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.login.ItemDetailsFragment"
            android:label="@string/title_activity_item_details_fragment" />
        <activity android:name=".Assignment1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>